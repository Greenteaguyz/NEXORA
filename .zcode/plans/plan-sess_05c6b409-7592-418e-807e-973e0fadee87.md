# Profile Stats Row — Unify Cards (Intent-Layer)

**Diagnosis:** the 4 stat cards are inconsistent — the Wallet card has no defined `.emerald` tile class (its icon renders bare), its "value" is the word "Wallet" instead of data, its subtitle wraps 3 lines, and "Orders Completed" wraps while others don't.

**Goal:** one anatomy for all 4 cards: colored icon tile + big value (real data) + single-line label + chevron.

**Change (profile.component.ts/.html/.css):**

1. **Wallet card becomes data-driven**: value = **actual balance** (`$12.75` for Bob), label = "Wallet Balance". Wire via existing `PAYMENTS_DATA.getWalletSnapshot` + `formatUsd` (both already in the codebase).
2. **Add the missing `.stat-icon-wrap.emerald`** tile style (green, token-based like lime/rose/cyan).
3. **Labels single-line**: "Games Owned" / "Wishlisted" / "Orders" / "Wallet Balance" (`white-space: nowrap` on `.stat-title`).
4. No TS outside profile component; no behavior changes; cards stay links.

**AC:**
- AC-1: all 4 cards identical anatomy; wallet icon inside an emerald tile.
- AC-2: wallet card shows real formatted balance as its value.
- AC-3: labels never wrap; no layout shift (card heights equalize).
- AC-4: stats load per user as before (multi-persona intact).

**Verification:** `npm run verify` + screenshot of the cleaned stats row.